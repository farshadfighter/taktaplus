services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-taktaplus}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-taktaplus}
      POSTGRES_DB: ${POSTGRES_DB:-taktaplus}
    volumes:
      - taktaplus_db:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    restart: unless-stopped

  mock-license-server:
    build: ./dev/mock-license-server
    restart: unless-stopped
    ports:
      - "8100:8100"

  backend:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    volumes:
      - master_key:/etc/taktaplus
      - packages_data:/var/lib/taktaplus/packages
    depends_on:
      - db
      - redis
      - mock-license-server
    ports:
      - "8000:8000"

  worker:
    build: ./backend
    restart: unless-stopped
    command: celery -A app.workers.celery_app worker --loglevel=info
    env_file: .env
    volumes:
      - master_key:/etc/taktaplus
      - packages_data:/var/lib/taktaplus/packages
    depends_on:
      - db
      - redis

  beat:
    build: ./backend
    restart: unless-stopped
    command: celery -A app.workers.celery_app beat --loglevel=info
    env_file: .env
    volumes:
      - master_key:/etc/taktaplus
    depends_on:
      - db
      - redis

  snmp-trap-receiver:
    build: ./backend
    restart: unless-stopped
    command: python -m app.workers.snmp_trap_receiver
    env_file: .env
    volumes:
      - master_key:/etc/taktaplus
    depends_on:
      - db
    ports:
      - "162:162/udp"

  ftp-relay:
    build: ./backend
    restart: unless-stopped
    command: python -m app.workers.ftp_relay_server
    env_file: .env
    volumes:
      - master_key:/etc/taktaplus
      - packages_data:/var/lib/taktaplus/packages
    ports:
      - "21:21"
      - "60000-60020:60000-60020"

  radius-server:
    build: ./backend
    restart: unless-stopped
    command: python -m app.workers.radius_server
    env_file: .env
    volumes:
      - master_key:/etc/taktaplus
    depends_on:
      - db
    ports:
      - "1812:1812/udp"

  frontend:
    build: ./frontend
    restart: unless-stopped
    environment:
      VITE_BACKEND_URL: http://backend:8000
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  taktaplus_db:
  master_key:
  packages_data:
